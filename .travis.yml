language: node_js
sudo: false
node_js:
  - 10
install:
  - npm install -g pnpm
  - pnpm install
script:
  - pnpm tslint
  - pnpm build
  - pnpm deploy
cache: pnpm
deploy:
  - provider: pages
    skip-cleanup: true
    local-dir: deploy
    github-token: $GITHUB_TOKEN
    on:
      branch: master
